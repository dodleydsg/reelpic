export default class Loader {
  constructor(pageSize, callback) {
    this._pageSize = pageSize;
    this._callback = callback; // callback should return an object containing the requested data
  }

  


}
